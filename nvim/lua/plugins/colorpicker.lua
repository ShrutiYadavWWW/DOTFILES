return {
  "uga-rosa/ccc.nvim",

  -- local ccc = require("ccc")
  -- local mapping = ccc.mapping

  opts = {
    setup = {
      highlighter = {
        auto_enable = true,
        lsp = true,
      },
    },
  }
}
